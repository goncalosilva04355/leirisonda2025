[build]
  publish = "dist"
  command = "npm ci && npm run build"

[build.environment]
  NODE_VERSION = "20"
  NETLIFY_NEXT_PLUGIN_SKIP = "true"
  VITE_IS_NETLIFY = "true"

# Basic SPA redirect
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
