// Config Fixed - DISABLED (Firestore not available)
console.log("🚫 Config fixed disabled - Firestore not available");

export const app = null;
export const db = null;
export const auth = null;
export const storage = null;

export const isFirebaseReady = () => false;
export const isFirebaseHealthy = () => false;
