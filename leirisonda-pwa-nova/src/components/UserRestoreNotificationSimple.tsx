// Versão simples do UserRestoreNotification sem useState problemático
import React from "react";

const UserRestoreNotificationSimple: React.FC = () => {
  // Versão estática sem state
  return null; // Não mostrar por enquanto
};

export default UserRestoreNotificationSimple;
