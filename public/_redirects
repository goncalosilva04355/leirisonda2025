# PWA manifest and service worker (must come before SPA rule)
/manifest.json    /manifest.json    200
/sw.js           /sw.js            200

# API proxy rules (if any future API endpoints are added)
/api/*           /api/:splat       200

# SPA redirect rule for client-side routing (must come last)
/*    /index.html   200
